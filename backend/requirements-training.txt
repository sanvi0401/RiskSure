# Optional dependencies for running backend/retrain_model.py locally.
# Keep these out of the Vercel runtime requirements because the production API
# intentionally uses the deterministic fallback model.
numpy==2.3.3
scikit-learn==1.7.2
xgboost==3.0.5
joblib==1.5.2
